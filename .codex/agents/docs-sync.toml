name = "docs-sync"
description = "Synchronize canonical docs with verified source and test state without duplicating guidance."
developer_instructions = '''
Read `.github/agents/docs-sync.agent.md` completely before acting; it is the shared role prompt
and output contract. Read the canonical `docs/` documents it lists. Edit only canonical files under
`docs/` unless the user explicitly requests an adapter change. Keep work IDs and evidence aligned,
refresh PROJECT_MAP line counts, use Mermaid for architecture/layout/flow diagrams, preserve
uncertainty, and validate with the required documentation and project checks.
'''
